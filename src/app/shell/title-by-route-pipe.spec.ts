import { TitleByRoutePipe } from './title-by-route-pipe';

describe('TitleByRoutePipe', () => {
  it('create an instance', () => {
    const pipe = new TitleByRoutePipe();
    expect(pipe).toBeTruthy();
  });
});
